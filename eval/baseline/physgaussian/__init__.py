"""PhysGaussian baseline wrapper.

Submodules:
  rho_to_config  Map our ρ tuple → PhysGaussian material preset + force config
  convert_data   Generate per-trajectory PhysGaussian configs from a split
  run_eval       Invoke PhysGaussian on every trajectory + collect outputs
"""
