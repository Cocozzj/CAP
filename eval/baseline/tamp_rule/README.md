# DEPRECATED

Replaced by `eval/baseline/tamp_pddl/` (PDDLStream-based TAMP).

The hand-coded rule baseline was a stop-gap; the paper now compares against
full Task and Motion Planning via the PDDLStream library.

Safe to delete:
```bash
rm -rf eval/baseline/tamp_rule
```
