"""DEPRECATED — symbolic action parsing now lives in eval.baseline.tamp_pddl."""
import warnings
warnings.warn("eval.baseline.tamp_rule.parser is deprecated; use eval.baseline.tamp_pddl",
                DeprecationWarning, stacklevel=2)
